<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Admin - Casa Monarca</title>

    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #f5f5f5;
        }

        /* HEADER */
        .topbar {
            background: #1f1f1f;
            color: white;
            padding: 10px 40px;
            display: flex;
            justify-content: space-between;
        }

        .info-bar {
            background: #eeeeee;
            padding: 20px 40px;
            display: flex;
            justify-content: space-around;
        }

        /* CONTENEDOR */
        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 80vh;
        }

        /* CAJA NARANJA */
        .admin-box {
            background: #e0b07a;
            width: 700px;
            height: 450px;
            border-radius: 30px;
            padding: 20px;
            display: flex;
            flex-direction: column;
        }

        h2 {
            text-align: center;
        }

        /* SCROLL INTERNO */
        .data-container {
            overflow-y: auto;
            padding-right: 10px;
        }

        /* TARJETAS */
        .card {
            background: white;
            padding: 10px;
            margin: 10px 0;
            border-radius: 15px;
            font-size: 14px;
        }

        /* SCROLL BONITO */
        .data-container::-webkit-scrollbar {
            width: 6px;
        }

        .data-container::-webkit-scrollbar-thumb {
            background: #888;
            border-radius: 10px;
        }

    </style>
</head>
<body>

<!-- HEADER -->
<div class="topbar">
    <div>CASA MONARCA - ADMIN</div>
    <div>👤 Admin</div>
</div>

<div class="info-bar">
    <div>Lunes a Domingo<br>08:00am - 6:00pm</div>
    <div>Contacto<br>(+52) 81 4193 7820</div>
    <div>Email<br>contacto@casamonarca.org.mx</div>
</div>

<!-- CONTENIDO -->
<div class="container">

    <div class="admin-box">

        <h2>Datos de Encuestas</h2>

        <div class="data-container">

            {% for d in datos %}
                <div class="card">
                    {{ d }}
                </div>
            {% endfor %}

        </div>

    </div>

</div>

</body>
</html>